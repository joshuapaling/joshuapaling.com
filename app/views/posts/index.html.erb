<% if @category %>
  <% pageTitle = 'Listing Posts in ' + @category.name %>
<% else %>
  <% pageTitle = 'Listing All Posts' %>
<% end %>

<h1><%= pageTitle %></h1>

<%= paginate @posts %>

<%= render :partial => @posts %>

<%= paginate @posts %>
